export type Environment = {
  NODE_ENV: string;
  PORT: number;
  ENABLE_SWAGGER: boolean;
  FIREBASE_CREDENTIALS: string;
  TYPEORM_HOST: string;
  TYPEORM_PORT: number;
  TYPEORM_USERNAME: string;
  TYPEORM_PASSWORD: string;
  TYPEORM_DATABASE: string;
  TYPEORM_CLOUDSQL: string;
};
